export type TTrip = {
  userId: string,
  tripDetails: string,
  startingPoint: string,
  destination: string,
  startDate: Date,
  endDate: Date,
  budget: number,
  activities: string[],
  capacity: number,
};
